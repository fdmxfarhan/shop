doctype html

html
    head
        title Register
        link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css")
        link(rel="shortcut icon", href="/img/logo.png", type="image/x-icon")
        script(src="/js/jquery-3.5.1.min.js")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel='stylesheet', href='/css/users.css')
        link(rel="stylesheet", href="/css/components/messages.css")
    body
        include ./components/messages
        
        .reg-area
            .image
                img(src="/img/users/bg-2.jpg", alt="")
            .register
                h1 ورود
                h2 برای ورود شماره موبایل خود را وارد نمایید
                form(action="/users/login" id="register" method="POST")
                    input.textinput(type="text" placeholder="کد ملی" name="username" value= "#{ typeof uname != 'undefined' ? uname: '' }")
                    input.textinput(type="checkbox" name="rules" id="rules")
                    label(for="rules") 
                        a(href="#") شرایط و قوانین
                        | را می‌پذیرم.
                    button.submit(type="submit" ) ارسال رمز یک بار مصرف
                    a.link(href="/users/register") ثبت نام