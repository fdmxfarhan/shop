doctype html

html
    head
        title Register
        link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css")
        link(rel="shortcut icon", href="/img/logo.png", type="image/x-icon")
        script(src="/js/jquery-3.5.1.min.js")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel='stylesheet', href='/css/users.css')
        link(rel="stylesheet", href="/css/components/messages.css")
    body
        include ./components/messages
        
        .reg-area
            .image
                img(src="/img/users/bg-2.jpg", alt="")
            .register
                h1 تایید شماره موبایل
                h2 کد پیامک شده را وارد کنید
                form(action="/users/enter-code" id="register" method="POST")
                    input.hidden(type="text" name="phone" value="#{phone}")
                    input.hidden(type="text" name="code" value="#{code}")
                    input.textinput(type="text" placeholder="* * * *" name="enterCode" required)
                    br
                    button.submit(type="submit" ) تایید شماره موبایل
                    a.link(href="/users/login") 
                        i.fa.fa-user-plus
                        | ثبت نام

        script(src="/js/numberFixer.js") 
